Create a GUI for the app (casting videos from local device)
It shoud be native MacOs app
it should look like Far Manager - the idea of navigating folder on MacOS at the left pane, to find the videofile to cast. In the right pane - the list of devices avilable for casting. I have to select the right device, hit enter - and it opens like an empty folder. Then I "copy" the file (by pushing F5 or something like in far manager) to this folder and the cast begins. To stop casting I have to go out of the device "folder"
If during "copying" the file (casting) the app needs to transcode or something else - it shows the confirmation window in Far style and then do the transcoding showing the progress bar
there is shoild be a way to reload the device list (in Far Manager there was a shortkeys list in the bottom of the window)
You can consider additional useful features for yourself